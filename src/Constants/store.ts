import { Language } from 'Constants/ids'

export const KEY_I18N = 'i18n'
export const KEY_POLICY_ACCEPTED = 'policyAccepted'

export const DEFAULT_STATES = {
  KEY_I18N: { locale: Language.Ru },
  KEY_POLICY_ACCEPTED: false,
}
